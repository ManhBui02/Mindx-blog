#Blog app using Reactjs <br />
``git clone https://github.com/yukirelia/mindx-blog.git`` to download the source <br />
``npm install`` to initialize and install the dependency <br />
``npm run start`` to start the react server <br />
